#!/usr/bin/python3
def uppercase(str):
    for i in range(len(str)):
        s = ord('str[i]')
        if s >= ord('a') and s <= ord('z'):
            str[i] == chr(s - 32)
    print("{}.format(str)")
